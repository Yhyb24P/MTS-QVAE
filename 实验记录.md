
**项目目标：** 研发并严格对比两种深度生成模型架构——**Standard VAE (基于高斯先验)** 和 **QBM-VAE (基于 RBM 先验)**——在生成高质量、新颖且多样化的线粒体靶向序列 (MTS) 方面的性能。

**最终结论（概览）：** 实验首先建立了一个强大的基线：**Standard VAE (高斯先验)** 架构，该架构复现了论文的结果，在结构多样性和功能性（例如 90.14%）上均取得**巨大成功**。

在此基础上，项目演进到更强大的 **QBM-VAE (RBM 先验)** 架构，以探索其是否能提供更高的性能或新颖性。在解决了所有底层技术障碍（算法 Bug、硬件瓶颈、CUDA 冲突）后，QBM-VAE ($\beta=1.0$) 陷入“区域坍塌”（即“低复杂度陷阱”）。

最终，通过**两项关键优化**——引入 `BETA = 0.1`（架构优化）和调整 `SimulatedAnnealingOptimizer` 参数（求解器调优）——成功解决了此问题。下游基准测试证实，**经过优化的** $\beta$**-QBM-VAE (RBM 先验) 架构**在“线粒体成功率”和“新颖性”两方面均**显著优于**成功的 Standard VAE 基线，是此项任务的最终成功方案。

## 第 1 部分：实验阶段一：Standard VAE (高斯先验) 基线建立

**实验目的：** 复现并评估 `MTS-VAE.pdf` 论文中的 Standard VAE (高斯先验) 架构，作为项目的“黄金标准”基线。

- **执行脚本：**
    
    - `train.py (Standard VAE 优化版)`
        
    - `generate.py (Standard VAE 优化版)`
        
- **关键架构与参数：**
    
    - **架构 (Architecture):** 全连接层 (Fully Connected)
        
    - **先验 (Prior):** `N(0, I)` (标准高斯分布)
        
    - **潜空间 (Latent Dim):** 32
        
    - **优化器 (Optimizer):** `optim.Adam`
        
    - **学习率 (Learning Rate):** `1e-3`
        
    - **批大小 (Batch Size):** `512`
        
    - **损失 (Loss):** `BCE + KLD`
        
    - **性能:** 使用 `DataLoader` 和 `.to(device)` 充分利用 GPU。
        
- **观测结果 (实验成功)：**
    
    - **结构多样性：** 对![[聚类分析 3.jpg]]显示，由 Standard VAE 生成的 'x' 标记（人工序列）**均匀且深入地分布在所有四个物种簇** (`HUMAN`, `TOBAC`, `YEAST`, `Rhoto`) 中。对 `聚类分析表.csv` 原始数据的抽样检查证实，表中 `org_label` 列包含了所有四种标签。
        
    - **功能性：** 此工作流在下游验证中取得了极高的成功率（90.14%）。
        
- **结论：** Standard VAE (高斯先验) 架构是一个非常成功的基线模型。
    
- **后续行动：** 探索 QBM-VAE (RBM 先验) 架构是否能在此成功基线上进一步提升性能。
    

## 第 2 部分：架构演进：转向 QBM-VAE (RBM 先验)

**实验目的：** 鉴于 Standard VAE 已取得成功，将模型架构升级到更强大的 QBM-VAE，以探索 RBM 先验是否能捕捉更复杂的分布，从而生成更优或更具新颖性的序列。

- **动机：**
    
    1. 标准 VAE 假设潜变量 $z$ 遵从一个简单的、固定的高斯先验 $\mathcal{N}(0, I)$。
        
    2. 这是一个“单峰”分布，可能无法完全捕捉真实世界数据（如蛋白质序列空间）中复杂的、**多模态 (multi-modal)** 的内在结构。
        
    3. **核心思想：** 用一个更强大的、**可学习的玻尔兹曼机 (RBM)** 分布来替换简单的高斯先验，以期获得更高的性能。
        
- **关键技术转变：**
    
    1. **核心库：** 从纯 PyTorch 转向 `kaiwu_torch_plugin`。
        
    2. **潜变量先验：** 从固定的高斯分布 $\mathcal{N}(0, I)$ 变为**可学习的** `RestrictedBoltzmannMachine` (RBM)。
        
    3. **训练逻辑：** 从单一梯度流变为**双重梯度流** (同时优化 VAE 参数 + RBM 先验参数)。
        
    4. **采样器：** 从高斯再参数化 (`torch.randn`) 变为 `SimulatedAnnealingOptimizer` (模拟退火)。
        

## 第 3 部分：QBM-VAE 管道底层技术调试 (Train & Generate)

为使**新的 QBM-VAE 模型**“跑起来”所进行的、与算法无关的底层算法 Bug 和工程性能修复。

### 3.1: 训练脚本 (train.py) 调试 - 解决“算法 Bug”

- **问题：** 初始 QBM-VAE 训练后，5000 次采样仅生成 **24 条**独特的序列（严重模式塌陷）。
    
- **诊断 (Bug B)：** `train.py` 脚本**只定义了一个优化器** (`optimizer_vae`)。RBM 先验的参数 $\{W, h\}$ **从未被训练**。
    
- **解决方案 (Fix 1)：**
    
    - **参数改动：**
        
        1. 创建**两个**独立的优化器：`optimizer_vae = optim.Adam(..., lr=1e-4)` 和 `optimizer_bm = optim.Adam(..., lr=1e-5)`。
            
        2. 在训练循环中实现**两个**独立的 `backward()` 调用（`vae_loss.backward()` 和 `bm_loss.backward()`）。
            
- **结果：** 独特序列从 24 条**显著增加**到 **2687** 条。但 `BM Loss` 仍不稳定。
    

### 3.2: 训练脚本 (train.py) 调试 - 解决“硬件瓶颈”

- **问题：** `nvidia-smi` 显示 GPU 利用率仅为 **1%-5%**。
    
- **诊断 1：** `BATCH_SIZE = 512` 过小。
    
- **参数改动 1：** `BATCH_SIZE` $\rightarrow$ **`2048`**。
    
- **问题 2：** GPU 利用率**再次降至 1%**。
    
- **诊断 2：** `SimulatedAnnealingOptimizer` 在单进程模式下成为“CPU 瓶颈”。
    
- **参数改动 2：** `SimulatedAnnealingOptimizer` $\rightarrow$ `SimulatedAnnealingOptimizer(..., **process_num = -1**)`。
    
- **问题 3：** 训练在 **Epoch 33** 突然**崩溃**（损失反弹）。
    
- **诊断 3：** 学习率 (LR) 在训练后期过高。
    
- **参数改动 3：** 引入 `lr_scheduler.ReduceLROnPlateau` 监控验证损失。
    

### 3.3: 生成脚本 (generate.py) 调试 - 解决“CUDA 冲突”

- **问题：** 运行 `generate.py` (已启用 `process_num = -1`) 崩溃，`RuntimeError: Cannot re-initialize CUDA in forked subprocess.`。
    
- **诊断：** `fork` 启动模式与 PyTorch CUDA 上下文冲突。
    
- **解决方案 1：** 在脚本顶部添加 `mp.set_start_method('spawn', force=True)`。
    
- **问题 2：** 再次崩溃，`AttributeError: '...RestrictedBoltzmannMachine' object has no attribute 'shape'`。
    
- **诊断 2：** 错误地调用了 `model.sampler.solve(model.bm)`。
    
- **解决方案 2 (最终)：** 通过分析 `kaiwu` 源码，发现正确的 API 调用是 `model.bm.sample(model.sampler)`。
    

**阶段结论：** 此时，QBM-VAE 模型在**技术上**已完全调通并可稳定运行。

## 第 4 部分：实验阶段二：QBM-VAE ($\beta=1.0$) 评估与多样性调试

**实验目的：** 使用**已完全调试好**的 QBM-VAE 管道（`BETA=1.0`），评估其**初始算法性能**。

- **执行脚本：** `train.py` (QBM-VAE 优化版, $\beta=1.0$), `generate.py` (MCMC 采样版), `sample.py`
    
- **观测与分析（1/3：训练输出）：**
    
    - **损失曲线 (![[loss_curves_bs2048.png]])：**
        
        - **ELBO Loss / Cost (上/中图)：** `Train` (蓝线) 和 `Valid` (红线) 曲线均平滑下降并收敛。这表明模型的编码器和解码器在**重建任务**上表现良好。
            
        - **BM CD Loss (下图)：** `Train BM CD Loss` (绿线) 在训练早期（约 10 个 Epochs）下降后，**开始不稳定地持续上升**，从约 0.1 攀升至 0.25 以上。这**清晰地表明** RBM 先验的训练（双重梯度流的第二部分）**已经发散**，未能学习到真实的分布。
            
    - **RBM 权重 (![[rbm_weights_heatmap_epoch_final_bs2048 2.png]])：**
        
        - 热力图显示，最终的 RBM 权重矩阵 W **几乎全为零**（呈现单一的深色）。这与第 5 部分中 `beta=0.1` 的有结构的热力图形成鲜明对比，**在视觉上证实** RBM 先验因训练发散而**完全失败**，未能学习到任何有意义的特征。
            
- **观测与分析（2/3：生成输出，“区域坍塌”）：**
    
    - **日志分析：** `generate.py` 脚本日志显示，此次运行（$\beta=1.0$, MCMC 采样）共生成 826 条唯一序列，`sample.py` 脚本从这 826 条序列中筛选出 214 条。
        
    - **可视化分析** (![[聚类分析 2.jpg]]): UMAP 图显示，这 214 条通过筛选的序列（'x' 标记）全部聚集在第 2 簇 (Rhoto) 的一个极小区域内。模型完全未能生成对应 `HUMAN`、`TOBAC` 或 `YEAST` 簇的任何序列。
        
    - **数据分析 (`聚类分析表.csv`)：** 对 CSV 文件的抽样检查证实了可视化分析。`name` 列为 `qvae_sample_...` 的每一行，其 `org_label` 列 **全部** 为 `Rhoto`。
        
- **根本原因分析 (RCA)：** 定位到两个并行的根本原因：
    
    1. **原因 A (求解器)：** `kaiwu.classical.SimulatedAnnealingOptimizer` 的**参数配置不当**（冷却过快），导致在训练和生成时均陷入局部最优解（即 `Rhoto` 簇）。
        
    2. **原因 B (架构)：** `BETA = 1.0` 的 KL 散度惩罚过强。
        
        - **数据审查 (`distribution.py`)：** 发现 `Rhoto`/`YEAST` 仅占数据的 10%（极端少数类）。
            
        - **诊断 (“低复杂度陷阱”)：** $\beta=1.0$ 迫使 RBM 先验“偷懒”（在发散的训练中寻找最简单的能量谷底），放弃学习 90% 的复杂数据，转而只拟合 10% 结构最简单的模式。
## 第 5 部分：实验阶段三：最终优化与基准测试 (求解器调优 + $\beta$-VAE)

**实验目的：** 修复在第 4 部分中发现的“区域坍塌”问题。此阶段通过两项并行且关键的优化（A: 求解器调优, B: $\beta$-VAE 架构优化）来解决两个根本原因，并对最终成功的模型进行下游基准测试。

- **执行脚本：**
    
    - train.py (最终版 $\beta=0.1$ )`
        
    - generate.py (最终版 $\beta=0.1$)`
        
    - `deeploc2.py` (用于基准测试)
        
    - `seq_identity.py` (用于新颖性分析)
        
- **关键架构与参数变更：**
    
    - **架构 (Architecture):** 1D-CNN Encoder/Decoder 
        
    - **先验 (Prior):** `RestrictedBoltzmannMachine` (RBM)
        
    - **A - 求解器调优 (SimulatedAnnealingOptimizer)：**
        
        - `initial_temperature`: 10.0 $\rightarrow$ **100.0** （提高初始温度）
            
        - `alpha`: 0.9 $\rightarrow$ **0.99** （减慢降温速率）
            
        - `iterations_per_t`: 10 $\rightarrow$ **100** （增加每个温度下的迭代次数）
            
    - **B - 架构优化 (**$\beta$**-VAE)：**
        
        - `BETA` ($\beta$ 系数): 1.0 $\rightarrow$ **0.1** (降低 KL 散度惩罚)
            
        - `LATENT_DIM`: 32 $\rightarrow$ **64** (扩大潜空间容量)
            
    - **C - 性能参数 ：**
        
        - `BATCH_SIZE`: **2048**
            
        - `process_num`: **-1**
            
        - 优化器: `optimizer_vae` (LR: `1e-4`), `optimizer_bm` (LR: `1e-5`)
            
        - 调度器: `ReduceLROnPlateau`
            
- **观测与分析（1/3：训练输出）：**
    
    - **损失日志 (`loss_qvae_cnn_bs2048_ld64_beta0.1.txt`)：** 日志显示训练过程稳定。ELBO (VAE Loss) 和 BM Loss (对比散度) 均在正常范围内波动和收敛。例如，在 Epoch 47，`Train BM Loss: 0.0655`，`Valid BM Loss: 0.1983`，未见发散迹象。RBM 权重参数（W 和 h）的均值和标准差均保持在合理的非零范围，表明 RBM 先验在持续学习。
        
    - **损失曲线 (![[loss_curves_bs2048_ld64_beta0.1.png]])：**
        
        - **ELBO Loss / Cost (上/中图)：** `Train` (蓝线) 和 `Valid` (红线) 曲线均平滑下降并在约 20 个 Epochs 后收敛。两条曲线紧密贴合，**未见明显过拟合**。
            
        - **BM CD Loss (下图)：** `Train BM CD Loss` (绿线) 在训练早期（前 10-15 个 Epochs）显著下降，随后在 0 附近（约 -0.2 到 0.2 之间）稳定震荡，**表明 RBM 先验的训练已达平衡**，成功避免了实验 3.1 中的发散问题。
            
    - **RBM 权重 (![[rbm_weights_heatmap_epoch_final_bs2048_ld64_beta0.1.png]])：** RBM 权重热力图显示，最终的权重矩阵呈现出**清晰、非零、有结构的模式**。这与实验 3.1 中记录的未训练（或不佳）的权重矩阵（近乎全零）形成鲜明对比，**在视觉上证实了 RBM 先验已成功学习**。
        
- **观测与分析（2/3：功能预测）：**
    
    - **分析 (`image_d971c7.jpg` DeepLoc 2.0 预测表)：**
        
        - 抽样分析预测表，以 `qvae_sample_` 开头的序列（来自 $\beta$-QBM-VAE）被**高度预测为 'Mitochondrion'（随机选出470个amts，仅有两个被预测为胞外信号肽）**。
            
        - 例如，`qvae_sample_982` (ID 983) 的 'Mitochondrion' 预测概率高达 **0.9995**。
            
        - `qvae_sample_980` (ID 981) 的概率为 **0.9986**。
            
        - `qvae_sample_979` (ID 980) 的概率为 **0.9994**。
            
        - 结果表明，最终的 $\beta$-QBM-VAE 模型生成的序列具有极高的功能性（线粒体靶向性）。
            
    - 使用 `filter_sequences.py` 脚本，可根据 TargetP 或 DeepLoc 2.0 预测结果（例如 `mTP` 概率 > 0.8）自动筛选出“黄金标准”候选集。
        
- **观测与分析（3/3：下游基准测试）：**
    
    - **分析** `image_f9edc9.jpg` **（线粒体成功率对比)：**
        
        - $\beta$-QBM-VAE（标记为 VAE-RBM (Ours)）的成功率（橙色条）在所有工具（TargetP, DeepLoc 1.0, DeepLoc 2.0）上均**显著高于** Standard VAE（蓝色条）、pHMM（灰色条）和 ModLamp（黄色条）。
            
    - **分析** ![[聚类分析 1.jpg]]**(新颖性/身份距离对比)：**
        
        - $\beta$-QBM-VAE（橙色分布）的同一性（Identity）分布**显著偏左**（即与训练集更不相似），其峰值集中在 0.3-0.4 之间。
            
        - Standard VAE（蓝色分布）的峰值则集中在 0.4-0.5 之间。
            
- **结论 (最终)：**
    
    1. **调试成功：** 必须**同时**实施（A）求解器调优和（B）$\beta$-VAE 架构优化，才能解决 QBM-VAE ($\beta=1.0$) 的“区域坍塌”问题。
        
    2. **性能超越：** 下游基准测试证实，最终的 $\beta$-QBM-VAE (RBM 先验) 架构在“线粒体成功率”和“新颖性”两方面均**显著优于** Standard VAE 基线。
        
    3. **最终方案：** $\beta$-QBM-VAE 是此项任务的最终成功方案。
        
---

